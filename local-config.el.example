;;; local-config.el.example --- Local configuration template -*- lexical-binding: t -*-

;;; Commentary:
;; This file contains machine-specific settings that should not be committed to git.
;;
;; SETUP INSTRUCTIONS:
;; 1. Copy this file to local-config.el:
;;    cp local-config.el.example local-config.el
;;
;; 2. Edit local-config.el and set the `user-org-directory` variable
;;
;; 3. The local-config.el file is gitignored and will not be committed

;;; Code:

;; Define the location of your org-mode files
;; Update this path to match your system
(defvar user-org-directory "/Users/YOUR-USERNAME/Documents/org/")

;; Provide feature
(provide 'local-config)
;;; local-config.el.example ends here
